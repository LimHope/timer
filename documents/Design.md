# 🎨 적응형 스터디 타이머 - Design Guide

## 문서 정보
- **버전**: v1.0
- **최종 수정일**: 2026-01-29
- **작성자**: UI/UX Design Team
- **검토자**: Senior Product Designer, CTO

---

## 디자인 철학 (Design Philosophy)

> "조용한 학습 파트너처럼"

이 제품의 UI는 사용자를 재촉하거나, 성과를 판단하거나, 주의를 요구하지 않습니다.  
인터페이스는 **감정적으로 따뜻하지만 시각적으로 절제된** 방식으로 집중을 지원합니다.

### 핵심 원칙

1. **인간 중심**: 시스템 주도적이지만, 인간미 있는 톤 유지
2. **차분함 우선**: 공격적인 애니메이션이나 화려한 효과 지양
3. **신뢰 구축**: 일관된 색상과 타이포그래피로 안정감 제공
4. **집중 지원**: 시각적 소음 최소화, 필요한 정보만 표시

### 감성 키워드
- Warm (따뜻한)
- Calm (차분한)
- Human (인간적인)
- Supportive (지지하는)
- Focused (집중된)
- Quietly Motivational (조용히 격려하는)

---

## 1. 디자인 시스템 개요 (Design System Overview)

### 1.1 비주얼 참조 분석

제공된 참조 이미지를 기반으로 다음 특성을 추출했습니다:

**색상 구조:**
- 🟢 **딥 그린 (Deep Green)**: 신뢰와 안정감의 주 색상
- 🔴 **소프트 레드 (Soft Red)**: 강조와 에너지를 위한 액센트
- 🟡 **크림/베이지 (Cream/Beige)**: 따뜻한 배경 톤
- ⚫ **다크 뉴트럴 (Dark Neutral)**: 텍스트, 순수한 검정 아님

**일러스트레이션 스타일:**
- 손그림 느낌의 아웃라인 캐릭터
- 간단한 표정과 제스처
- 플랫하고 친근한 느낌
- 과도한 디테일 없음

**레이아웃 특성:**
- 명확한 카드/패널 분리
- 넉넉한 여백 (낮은 UI 밀도)
- 시각적 소음 없음
- 미니멀한 장식 효과

### 1.2 디자인 토큰 구조

```
Design Tokens
├── Color Palette
├── Typography Scale
├── Spacing System
├── Border Radius
├── Shadow Levels
└── Transitions
```

---

## 2. 컬러 팔레트 (Color Palette)

### 2.1 Primary Colors (주 색상)

| 용도 | Tailwind Variable | HEX | RGB | 사용 예시 |
|------|------------------|-----|-----|----------|
| Primary | `primary-600` | `#0F5C4D` | rgb(15, 92, 77) | 버튼, 타이머, 주요 액션 |
| Primary Hover | `primary-700` | `#0D4A3E` | rgb(13, 74, 62) | 버튼 호버 상태 |
| Primary Light | `primary-100` | `#D4EDE8` | rgb(212, 237, 232) | 배경 강조, 뱃지 |
| Primary Dark | `primary-900` | `#083A2F` | rgb(8, 58, 47) | 텍스트 on primary |

**사용 가이드:**
- 타이머 원형 프로그레스
- 주요 CTA 버튼 ("세션 시작", "집중하기")
- 집중도 높은 상태 표시
- 네비게이션 활성 상태

### 2.2 Accent Colors (강조 색상)

| 용도 | Tailwind Variable | HEX | RGB | 사용 예시 |
|------|------------------|-----|-----|----------|
| Accent | `accent-500` | `#D84A4A` | rgb(216, 74, 74) | 중요 알림, 통계 강조 |
| Accent Light | `accent-100` | `#FADADA` | rgb(250, 218, 218) | 경고 배경 |
| Accent Dark | `accent-700` | `#A93737` | rgb(169, 55, 55) | 액센트 텍스트 |

**사용 가이드:**
- 피로도 경고
- 중요한 통계 하이라이트
- 제한적으로만 사용 (전체 화면의 5% 이내)
- 절대 배경 전체에 사용 금지

### 2.3 Neutral Colors (중립 색상)

| 용도 | Tailwind Variable | HEX | RGB | 사용 예시 |
|------|------------------|-----|-----|----------|
| Background | `neutral-50` | `#F9F7F4` | rgb(249, 247, 244) | 메인 배경 |
| Surface | `neutral-100` | `#F2EFE9` | rgb(242, 239, 233) | 카드 배경 |
| Border | `neutral-200` | `#E5E1D8` | rgb(229, 225, 216) | 구분선 |
| Text Secondary | `neutral-500` | `#8B8679` | rgb(139, 134, 121) | 보조 텍스트 |
| Text Primary | `neutral-900` | `#2A2621` | rgb(42, 38, 33) | 주 텍스트 (검정 아님!) |

**사용 가이드:**
- `neutral-50`: 전체 페이지 배경
- `neutral-100`: 카드, 패널, 입력 필드
- `neutral-900`: 헤딩, 본문 텍스트
- 순수한 `#000000` 또는 `#FFFFFF` 사용 금지

### 2.4 Semantic Colors (의미론적 색상)

| 용도 | Tailwind Variable | HEX | RGB | 사용 케이스 |
|------|------------------|-----|-----|------------|
| Success | `success-500` | `#4CAF50` | rgb(76, 175, 80) | 세션 완료, 목표 달성 |
| Warning | `warning-500` | `#FF9800` | rgb(255, 152, 0) | 집중도 하락 알림 |
| Info | `info-500` | `#2196F3` | rgb(33, 150, 243) | 인사이트, 팁 |

### 2.5 Focus Score Colors (집중도 점수 색상)

| 점수 범위 | 색상 | HEX | 의미 |
|----------|------|-----|------|
| 8-10 | `primary-600` | `#0F5C4D` | 최고 집중 |
| 5-7 | `warning-500` | `#FF9800` | 보통 집중 |
| 1-4 | `accent-500` | `#D84A4A` | 집중도 낮음 |

### 2.6 Color Usage Matrix

| 요소 | Primary | Accent | Neutral | 비고 |
|------|---------|--------|---------|------|
| 타이머 | ✅ | ❌ | ❌ | 항상 primary |
| 버튼 | ✅ | △ | ✅ | 주요 액션은 primary |
| 배경 | ❌ | ❌ | ✅ | neutral-50/100만 |
| 텍스트 | △ | △ | ✅ | 대부분 neutral-900 |
| 아이콘 | ✅ | △ | ✅ | 문맥에 따라 |
| 일러스트 | ✅ | ✅ | ✅ | 혼합 사용 가능 |

---

## 3. 타이포그래피 시스템 (Typography System)

### 3.1 폰트 패밀리

**Primary Font (본문):**
```css
font-family: 'Pretendard Variable', -apple-system, BlinkMacSystemFont, system-ui, sans-serif;
```

**Monospace Font (타이머, 숫자):**
```css
font-family: 'JetBrains Mono', 'SF Mono', Monaco, monospace;
```

**사용 이유:**
- Pretendard: 한글 가독성이 뛰어나고 따뜻한 느낌
- JetBrains Mono: 숫자 가독성, 기술적 신뢰감

### 3.2 Type Scale

| 레벨 | Tailwind Class | Font Size | Line Height | Letter Spacing | 용도 |
|------|---------------|-----------|-------------|----------------|------|
| Display | `text-6xl` | 60px | 1.1 | -0.02em | 타이머 숫자 |
| H1 | `text-4xl` | 36px | 1.2 | -0.01em | 페이지 제목 |
| H2 | `text-3xl` | 30px | 1.3 | 0 | 섹션 제목 |
| H3 | `text-2xl` | 24px | 1.4 | 0 | 서브섹션 |
| H4 | `text-xl` | 20px | 1.4 | 0 | 카드 제목 |
| Body Large | `text-lg` | 18px | 1.6 | 0 | 강조 본문 |
| Body | `text-base` | 16px | 1.6 | 0 | 기본 본문 |
| Body Small | `text-sm` | 14px | 1.5 | 0 | 보조 정보 |
| Caption | `text-xs` | 12px | 1.4 | 0.01em | 주석, 레이블 |

### 3.3 Font Weights

| Weight | Tailwind Class | 값 | 용도 |
|--------|---------------|-----|------|
| Regular | `font-normal` | 400 | 기본 본문 |
| Medium | `font-medium` | 500 | 강조, 레이블 |
| Semibold | `font-semibold` | 600 | 버튼, 헤딩 |
| Bold | `font-bold` | 700 | 주요 헤딩 (제한적) |

**가이드:**
- 대부분의 텍스트는 `font-normal` (400)
- 버튼과 헤딩만 `font-semibold` (600)
- `font-bold` (700)은 특별한 경우만 (예: "집중도 9.5/10")

### 3.4 타이포그래피 컴포넌트

#### 타이머 디스플레이
```
Font: JetBrains Mono
Size: 60px (mobile: 48px)
Weight: 600
Color: primary-600
Letter-spacing: -0.02em
```

#### 페이지 헤딩
```
Font: Pretendard
Size: 36px (mobile: 28px)
Weight: 600
Color: neutral-900
Line-height: 1.2
```

#### 본문 텍스트
```
Font: Pretendard
Size: 16px
Weight: 400
Color: neutral-900
Line-height: 1.6
```

#### AI 코치 메시지
```
Font: Pretendard
Size: 16px
Weight: 400
Color: neutral-700
Line-height: 1.8
Italic: 가능 (따뜻한 톤)
```

### 3.5 텍스트 색상 사용 규칙

| 텍스트 타입 | 색상 | 대비율 | WCAG 준수 |
|------------|------|--------|-----------|
| 주요 텍스트 | `neutral-900` | 13.5:1 | AAA |
| 보조 텍스트 | `neutral-500` | 4.8:1 | AA |
| Primary 버튼 텍스트 | `white` on `primary-600` | 7.2:1 | AA |
| 링크 | `primary-600` | 6.5:1 | AA |

---

## 4. 일러스트레이션 & 아이콘 가이드라인

### 4.1 일러스트레이션 스타일

참조 이미지에서 추출한 특성:

**핵심 특징:**
- ✏️ 손그림 느낌의 아웃라인
- 👥 단순화된 인물 캐릭터
- 😊 친근한 표정 (과장되지 않음)
- 🎨 플랫 컬러 (그라디언트 없음)
- 📐 2-3px 두께의 일관된 선

**색상 사용:**
- 주로 `primary-600` (딥 그린)
- 악센트로 `accent-500` (소프트 레드)
- 배경은 `neutral-50` 또는 투명

**Do's (권장):**
- ✅ 간단한 제스처 (손 흔들기, 책 읽기)
- ✅ 2-3명의 캐릭터 그룹
- ✅ 명확한 아웃라인
- ✅ 최소한의 디테일

**Don'ts (지양):**
- ❌ 복잡한 배경
- ❌ 그라디언트, 그림자 효과
- ❌ 사실적인 디테일
- ❌ 과장된 표정이나 동작

### 4.2 일러스트레이션 사용 케이스

| 화면 | 일러스트 | 위치 | 목적 |
|------|---------|------|------|
| 온보딩 | 책 읽는 캐릭터 | 중앙 | 환영 메시지 |
| 빈 상태 (공부 세션 없음) | 커피 들고 있는 캐릭터 | 중앙 | 첫 세션 시작 유도 |
| 휴식 모드 | 휴식 중인 캐릭터 | 배경 | 편안함 전달 |
| 세션 완료 | 하이파이브 하는 두 캐릭터 | 모달 상단 | 축하 |
| 인사이트 (데이터 없음) | 돋보기 들고 있는 캐릭터 | 중앙 | 데이터 수집 필요 안내 |

### 4.3 아이콘 시스템

**아이콘 스타일:**
- 라인 아이콘 (Outline, not filled)
- 2px 선 두께
- 24×24px 기본 크기
- 둥근 모서리 (rounded caps)

**추천 아이콘 세트:**
- [Lucide Icons](https://lucide.dev) (일관성, 오픈소스)
- 또는 커스텀 제작 (참조 스타일 맞춤)

### 4.4 주요 아이콘 목록

| 기능 | 아이콘 | Lucide 이름 | 색상 |
|------|--------|------------|------|
| 세션 시작 | ▶️ | `Play` | primary-600 |
| 일시정지 | ⏸ | `Pause` | neutral-600 |
| 종료 | ⏹ | `Square` | neutral-600 |
| 챗봇 | 💬 | `MessageCircle` | primary-600 |
| 인사이트 | 📊 | `BarChart3` | neutral-700 |
| 설정 | ⚙️ | `Settings` | neutral-600 |
| 집중도 상승 | ⭐ | `TrendingUp` | primary-600 |
| 집중도 하락 | 📉 | `TrendingDown` | warning-500 |
| 휴식 | ☕ | `Coffee` | neutral-600 |
| 완료 | ✅ | `CheckCircle` | success-500 |

### 4.5 아이콘 사용 규칙

```
크기:
- Small: 16px (인라인 텍스트)
- Medium: 24px (기본)
- Large: 32px (주요 버튼)
- XLarge: 48px (빈 상태)

색상 우선순위:
1. neutral-600 (기본)
2. primary-600 (주요 액션)
3. warning-500 / accent-500 (경고)
```

### 4.6 일러스트레이션 제작 가이드

**벡터 소프트웨어:**
- Figma (권장)
- Adobe Illustrator

**레이어 구조:**
```
Illustration
├── Background (optional)
├── Character Outline
├── Character Fill
├── Details (minimal)
└── Accents (red/green)
```

**익스포트 설정:**
- SVG 형식 (확장성)
- PNG @2x (폴백)
- 최대 너비: 400px
- 투명 배경

---

## 5. 페이지 구현 (Page Implementations)

### 5.1 공부 모드 (Study Mode)

#### 5.1.1 레이아웃 구조

```
┌─────────────────────────────────────────┐
│         [Top Bar - Minimal]             │ ← 최소화 가능
├─────────────────────────────────────────┤
│                                         │
│           [과목: 수학]                   │ ← text-sm, neutral-600
│                                         │
│              ⏱ 23:45                    │ ← Display, primary-600
│         (원형 프로그레스)                 │
│                                         │
│        ⭐⭐⭐⭐⭐⭐⭐ 7/10                 │ ← 집중도 점수
│                                         │
│     ━━━━━━━━━━━━━━━━━━━━               │ ← 프로그레스 바
│       예상 종료: 오후 3:45               │ ← text-sm
│                                         │
│    [💬 AI 코치에게 질문하기]             │ ← 보조 버튼
│                                         │
│   [⏸ 일시정지]    [⏹ 세션 종료]        │ ← 액션 버튼
│                                         │
└─────────────────────────────────────────┘
```

#### 5.1.2 컬러 사용

| 요소 | 색상 | 이유 |
|------|------|------|
| 배경 | `neutral-50` | 눈의 피로 최소화 |
| 타이머 숫자 | `primary-600` | 주요 정보 강조 |
| 원형 프로그레스 | `primary-600` (채움) + `neutral-200` (배경) | 진행 상황 시각화 |
| 집중도 별 | `warning-500` (활성) + `neutral-200` (비활성) | 점수 시각화 |
| 보조 텍스트 | `neutral-500` | 계층 구조 |

#### 5.1.3 상태별 변화

**기본 상태 (집중 중):**
- 타이머 색상: `primary-600`
- 배경: `neutral-50`
- Top Bar: 표시

**집중 모드 (전체화면):**
- Top Bar: 숨김 (호버 시 나타남)
- 배경: `neutral-50` (변화 없음)
- 타이머만 중앙에 크게 표시

**일시정지:**
- 타이머 색상: `neutral-400` (회색조)
- "일시정지됨" 텍스트 표시
- 프로그레스 애니메이션 정지

#### 5.1.4 반응형 동작

**Desktop (1024px+):**
- 타이머 크기: 60px
- 원형 프로그레스: 280px 지름
- 버튼 2개 나란히 배치

**Mobile (< 768px):**
- 타이머 크기: 48px
- 원형 프로그레스: 200px 지름
- 버튼 세로 스택

### 5.2 휴식 모드 (Break Mode)

#### 5.2.1 레이아웃 구조

```
┌─────────────────────────────────────────┐
│                                         │
│     (파도 애니메이션 배경 - 전체)         │
│                                         │
│                                         │
│              🎵 4:32 🎵                 │ ← 카운트다운
│                                         │
│                                         │
│                                         │
│                                         │
│           [휴식 끝내기]                  │ ← 작은 버튼
│                                         │
└─────────────────────────────────────────┘
```

#### 5.2.2 컬러 사용

| 요소 | 색상 | 이유 |
|------|------|------|
| 배경 | `primary-600` (전체) | 공부 모드와 명확히 구분 |
| 애니메이션 | `primary-400` (밝은 그린) | 부드러운 움직임 |
| 타이머 숫자 | `white` | 높은 대비 |
| 음악 아이콘 | `primary-200` | 은은한 존재감 |
| 버튼 | `white` 아웃라인 + 투명 배경 | 비침입적 |

#### 5.2.3 애니메이션 스타일

**파도 애니메이션:**
- 느린 속도 (5-10초 주기)
- 불투명도 10-20%
- 부드러운 이징 (`ease-in-out`)
- 반복 무한

**구름 애니메이션 (대안):**
- 좌→우로 천천히 이동
- 불투명도 15%
- 여러 레이어 (깊이감)

**별 애니메이션 (대안):**
- 반짝임 효과 (fade in/out)
- 무작위 위치
- 2-4초 주기

**선택 가이드:**
- 오후/저녁: 파도 또는 구름
- 밤: 별
- 사용자 설정 가능

#### 5.2.4 음악 재생 표시

```
[재생 중]
🎵 음표 아이콘 - 살짝 움직임
"Lo-fi Beats" - 작은 텍스트

[일시정지]
⏸ 아이콘
음표 애니메이션 정지
```

### 5.3 인사이트 / 대시보드 (Insights / Dashboard)

#### 5.3.1 레이아웃 구조

```
┌─────────────────────────────────────────┐
│          📊 이번 주 학습 리포트           │ ← 헤딩
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐  │
│  │ 15h 30m │ │  32분   │ │ 7.2/10  │  │ ← 통계 카드
│  │총 학습   │ │평균세션  │ │평균집중  │  │
│  └─────────┘ └─────────┘ └─────────┘  │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │   [시간대별 집중도 히트맵]          │ │ ← 차트 카드
│  │                                   │ │
│  └───────────────────────────────────┘ │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │ 💡 AI 코치의 한마디                │ │ ← 인사이트 카드
│  │ "월요일 오후 3-5시 집중 최고!"     │ │
│  └───────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘
```

#### 5.3.2 카드 디자인

**통계 카드 (Stat Card):**
```
배경: neutral-100
패딩: 24px
Border Radius: 16px
Border: 1px solid neutral-200
Shadow: 없음 (플랫)

숫자:
- Font: JetBrains Mono
- Size: 32px
- Weight: 600
- Color: primary-600

레이블:
- Font: Pretendard
- Size: 14px
- Color: neutral-500
```

**차트 카드:**
```
배경: white
패딩: 32px
Border Radius: 16px
Border: 1px solid neutral-200

차트 색상:
- 주요 라인: primary-600
- 보조 라인: neutral-200
- 포인트: primary-600
- 그리드: neutral-100
```

**AI 인사이트 카드:**
```
배경: primary-50 (연한 그린)
패딩: 24px
Border Radius: 16px
Border: 2px solid primary-200
Icon: 💡 (좌측 상단)

텍스트:
- Font: Pretendard
- Size: 16px
- Color: neutral-900
- Line-height: 1.8
```

#### 5.3.3 차트 스타일 가이드

**라인 차트 (집중도 추이):**
- 선 색상: `primary-600`
- 선 두께: 3px
- 포인트: 원형, 8px 지름
- 그리드: `neutral-100`, 점선
- 축 레이블: `neutral-500`, 12px

**막대 차트 (일별 공부 시간):**
- 막대 색상: `primary-600`
- 막대 너비: 40px
- 모서리: 상단만 둥글게 (8px)
- 간격: 8px
- 호버: `primary-700` + 살짝 확대

**히트맵 (시간대별 집중도):**
- 낮은 집중: `neutral-100`
- 중간 집중: `primary-200`
- 높은 집중: `primary-600`
- 셀 크기: 24×24px
- 셀 간격: 4px
- Border Radius: 4px

#### 5.3.4 빈 상태 (Empty State)

```
┌─────────────────────────────────────────┐
│                                         │
│         [일러스트: 돋보기 캐릭터]         │
│                                         │
│        아직 충분한 데이터가 없어요        │ ← H3, neutral-700
│    공부를 시작하면 인사이트가 생성됩니다   │ ← Body, neutral-500
│                                         │
│       [첫 세션 시작하기]                 │ ← Primary 버튼
│                                         │
└─────────────────────────────────────────┘
```

---

## 6. 레이아웃 컴포넌트 (Layout Components)

### 6.1 Top Bar (상단 네비게이션)

#### 6.1.1 구조

```
┌────────────────────────────────────────────────────────┐
│ [로고] StudyFlow    [공부][휴식][인사이트]   [💬][⚙️][👤] │
└────────────────────────────────────────────────────────┘
```

#### 6.1.2 스펙

| 요소 | 사양 |
|------|------|
| 높이 | 64px |
| 배경 | `neutral-50` (메인 배경과 동일) |
| 하단 보더 | 1px solid `neutral-200` |
| 패딩 (좌우) | 24px (desktop), 16px (mobile) |

**로고:**
- Font: Pretendard Semibold
- Size: 20px
- Color: `primary-600`
- 클릭 시 → `/app/study`

**탭 버튼:**
- Font: Pretendard Medium
- Size: 16px
- Color: `neutral-600` (비활성)
- Active Color: `primary-600`
- Active Indicator: 하단 2px 밑줄
- 호버: `primary-100` 배경 (subtle)

**아이콘 버튼:**
- 크기: 24×24px
- 색상: `neutral-600`
- 호버: `primary-600` + `neutral-100` 배경
- 간격: 12px

#### 6.1.3 반응형 동작

**Desktop (1024px+):**
- 모든 요소 표시
- 가로 배치 유지

**Mobile (< 768px):**
- 로고 + 햄버거 메뉴
- 햄버거 클릭 → 사이드 드로어
- 아이콘 최소화 (프로필만)

### 6.2 카드 컴포넌트 (Card Component)

#### 6.2.1 기본 카드

```css
배경: neutral-100
패딩: 24px
Border Radius: 16px
Border: 1px solid neutral-200
Shadow: 없음 (플랫 디자인)
```

#### 6.2.2 카드 변형

| 타입 | 배경 | 보더 | 용도 |
|------|------|------|------|
| Default | `neutral-100` | `neutral-200` | 일반 콘텐츠 |
| Elevated | `white` | `neutral-200` | 강조 콘텐츠 |
| Primary | `primary-50` | `primary-200` | AI 인사이트 |
| Accent | `accent-50` | `accent-200` | 경고/알림 |

#### 6.2.3 카드 내부 구조

```
┌─────────────────────────────────┐
│ [아이콘] 제목           [액션]   │ ← 헤더 (optional)
├─────────────────────────────────┤
│                                 │
│         본문 콘텐츠              │
│                                 │
├─────────────────────────────────┤
│ [버튼]              [링크]      │ ← 푸터 (optional)
└─────────────────────────────────┘
```

### 6.3 버튼 컴포넌트 (Button Component)

#### 6.3.1 버튼 타입

| 타입 | 배경 | 텍스트 | 보더 | 용도 |
|------|------|--------|------|------|
| Primary | `primary-600` | `white` | 없음 | 주요 액션 |
| Secondary | `neutral-100` | `neutral-900` | `neutral-200` | 보조 액션 |
| Outline | 투명 | `primary-600` | `primary-600` | 덜 중요한 액션 |
| Ghost | 투명 | `neutral-600` | 없음 | 텍스트 링크형 |
| Danger | `accent-500` | `white` | 없음 | 삭제, 경고 |

#### 6.3.2 버튼 크기

| 크기 | 높이 | 패딩 (좌우) | Font Size | 용도 |
|------|------|------------|-----------|------|
| Small | 32px | 12px | 14px | 인라인 액션 |
| Medium | 40px | 16px | 16px | 기본 |
| Large | 48px | 24px | 18px | 주요 CTA |

#### 6.3.3 버튼 상태

**Default:**
```css
배경: primary-600
텍스트: white
Border Radius: 8px
```

**Hover:**
```css
배경: primary-700
커서: pointer
Transition: background 0.2s ease
```

**Active (클릭):**
```css
배경: primary-800
Transform: scale(0.98)
```

**Disabled:**
```css
배경: neutral-200
텍스트: neutral-400
커서: not-allowed
불투명도: 60%
```

**Loading:**
```css
배경: primary-600 (유지)
텍스트: 숨김
스피너: white, 중앙 정렬
```

### 6.4 입력 필드 (Input Field)

#### 6.4.1 텍스트 입력

```css
높이: 48px
배경: neutral-100
보더: 1px solid neutral-200
Border Radius: 8px
패딩: 12px 16px
Font: Pretendard, 16px
텍스트 색상: neutral-900
```

**상태:**

| 상태 | 보더 색상 | 배경 | 그림자 |
|------|----------|------|--------|
| Default | `neutral-200` | `neutral-100` | 없음 |
| Focus | `primary-600` | `white` | `0 0 0 3px primary-100` |
| Error | `accent-500` | `accent-50` | 없음 |
| Disabled | `neutral-200` | `neutral-50` | 없음 |

#### 6.4.2 레이블 & 헬퍼 텍스트

**레이블:**
```css
Font: Pretendard Medium
Size: 14px
Color: neutral-700
Margin-bottom: 8px
```

**헬퍼 텍스트:**
```css
Font: Pretendard Regular
Size: 14px
Color: neutral-500
Margin-top: 8px
```

**에러 메시지:**
```css
Font: Pretendard Regular
Size: 14px
Color: accent-600
Margin-top: 8px
아이콘: ⚠️ (앞에 배치)
```

### 6.5 모달 (Modal)

#### 6.5.1 구조

```
┌─────────────────────────────────┐
│ [제목]                    [×]   │ ← 헤더
├─────────────────────────────────┤
│                                 │
│         본문 콘텐츠              │
│                                 │
├─────────────────────────────────┤
│         [취소]  [확인]          │ ← 푸터
└─────────────────────────────────┘
```

#### 6.5.2 스펙

```css
배경: white
Border Radius: 16px
Max Width: 480px (desktop), 90vw (mobile)
패딩: 32px
Shadow: 0 20px 60px rgba(0,0,0,0.15)

Overlay:
- 배경: rgba(0,0,0,0.4)
- Backdrop Blur: 4px (선택)
```

#### 6.5.3 애니메이션

**열림:**
```css
from: opacity: 0, scale: 0.95
to: opacity: 1, scale: 1
duration: 0.2s
easing: ease-out
```

**닫힘:**
```css
from: opacity: 1, scale: 1
to: opacity: 0, scale: 0.95
duration: 0.15s
easing: ease-in
```

### 6.6 토스트 알림 (Toast Notification)

#### 6.6.1 구조

```
┌─────────────────────────────────┐
│ [아이콘] 메시지 텍스트      [×] │
└─────────────────────────────────┘
```

#### 6.6.2 타입별 스타일

| 타입 | 배경 | 아이콘 | 보더 |
|------|------|--------|------|
| Success | `success-50` | ✅ `success-500` | `success-200` |
| Error | `accent-50` | ❌ `accent-500` | `accent-200` |
| Warning | `warning-50` | ⚠️ `warning-500` | `warning-200` |
| Info | `info-50` | ℹ️ `info-500` | `info-200` |

#### 6.6.3 위치 & 동작

```
위치: 우측 상단
거리: 24px from top, 24px from right
Duration: 4초 (자동 닫힘)
최대 개수: 3개 (스택)

애니메이션:
- 슬라이드 인 (우측에서)
- 페이드 아웃
```

---

## 7. 인터랙션 패턴 (Interaction Patterns)

### 7.1 인터랙션 원칙

1. **차분함 우선**: 급하거나 공격적인 애니메이션 금지
2. **의도적 변화**: 상태 변화는 명확하고 예측 가능해야 함
3. **호버 최소화**: 호버 효과는 필수적인 경우만 사용
4. **피드백 제공**: 모든 액션에 시각적 피드백 (subtle)

### 7.2 애니메이션 타이밍

| 타입 | Duration | Easing | 용도 |
|------|----------|--------|------|
| Micro | 0.1-0.15s | `ease-out` | 버튼 클릭, 호버 |
| Short | 0.2-0.3s | `ease-in-out` | 모달, 드롭다운 |
| Medium | 0.4-0.6s | `ease-in-out` | 페이지 전환 |
| Long | 0.8-1.2s | `ease-in-out` | 휴식 모드 배경 |
| Very Long | 3-5s | `linear` | 타이머, 프로그레스 바 |

### 7.3 호버 효과 가이드

**버튼 호버:**
```css
배경 색상 변화 (약간 어둡게)
Transition: 0.2s ease
Transform: 없음 (스케일 금지)
```

**카드 호버 (필요 시):**
```css
Border 색상 변화 (primary-300)
Transition: 0.2s ease
Shadow 추가 금지 (플랫 유지)
```

**링크 호버:**
```css
텍스트 색상: primary-700
밑줄: 표시 또는 제거 (일관성 유지)
```

### 7.4 포커스 스타일

**키보드 포커스:**
```css
Outline: 2px solid primary-600
Outline Offset: 2px
Border Radius: 계승 (요소에 따라)
```

**예시:**
```
버튼: 2px solid primary-600, 2px offset
입력 필드: 3px shadow (primary-100)
링크: 1px dotted primary-600
```

### 7.5 로딩 상태

#### 7.5.1 스피너

```css
크기: 24px (medium), 32px (large)
색상: primary-600 (primary 버튼에선 white)
두께: 3px
애니메이션: 회전 (1s linear infinite)
```

#### 7.5.2 스켈레톤 로더

```css
배경: neutral-100
애니메이션: 좌→우 shimmer
Duration: 1.5s
Easing: ease-in-out
```

**사용 케이스:**
- 인사이트 대시보드 로딩
- 차트 데이터 로딩
- 채팅 히스토리 로딩

### 7.6 드래그 앤 드롭 (해당 시)

**Draggable 요소:**
```css
커서: grab (기본), grabbing (드래그 중)
불투명도: 0.8 (드래그 중)
그림자: 추가 (subtle)
```

**Drop Zone:**
```css
보더: 2px dashed primary-400
배경: primary-50
Transition: 0.2s ease
```

### 7.7 스크롤 동작

**스크롤 바:**
```css
너비: 8px
배경: neutral-100
Thumb: neutral-300
Thumb Hover: neutral-400
Border Radius: 4px
```

**Smooth Scroll:**
```css
scroll-behavior: smooth
scroll-padding-top: 80px (fixed header 고려)
```

---

## 8. 반응형 브레이크포인트 (Breakpoints)

### 8.1 브레이크포인트 정의

```scss
$breakpoints: (
  'mobile': 320px,   // 최소 지원
  'tablet': 768px,   // 태블릿
  'desktop': 1024px, // 데스크톱
  'wide': 1440px     // 와이드 스크린
);
```

### 8.2 디자인 우선순위

**Desktop-First Approach**
- 기본 디자인: 1024px 기준
- Mobile: 최적화 (단순화)
- Tablet: 기본 지원 (추가 작업 최소)

### 8.3 레이아웃 변화

#### 8.3.1 컨테이너 최대 너비

| 브레이크포인트 | Max Width | 패딩 (좌우) |
|---------------|-----------|-------------|
| Mobile (< 768px) | 100% | 16px |
| Tablet (768-1023px) | 100% | 32px |
| Desktop (1024-1439px) | 960px | 48px |
| Wide (1440px+) | 1200px | 64px |

#### 8.3.2 그리드 시스템

**Desktop:**
```
12 컬럼 그리드
Gap: 24px
```

**Tablet:**
```
8 컬럼 그리드
Gap: 16px
```

**Mobile:**
```
4 컬럼 그리드 (또는 단일 컬럼)
Gap: 12px
```

### 8.4 컴포넌트별 반응형 동작

#### 8.4.1 Top Bar

| 화면 크기 | 로고 | 탭 | 아이콘 | 기타 |
|----------|------|-----|--------|------|
| Desktop | 표시 | 표시 (가로) | 모두 표시 | - |
| Tablet | 표시 | 표시 (가로) | 최소화 | - |
| Mobile | 표시 | 햄버거 메뉴 | 프로필만 | 사이드 드로어 |

#### 8.4.2 타이머 (Study Mode)

| 화면 크기 | 타이머 크기 | 원형 지름 | 버튼 배치 |
|----------|------------|----------|----------|
| Desktop | 60px | 280px | 가로 (2개) |
| Tablet | 54px | 240px | 가로 (2개) |
| Mobile | 48px | 200px | 세로 (스택) |

#### 8.4.3 인사이트 대시보드

**Desktop:**
```
통계 카드: 3개 가로 배치
차트 카드: 전체 너비
인사이트 카드: 전체 너비
```

**Tablet:**
```
통계 카드: 3개 가로 (약간 작게)
차트 카드: 전체 너비
인사이트 카드: 전체 너비
```

**Mobile:**
```
통계 카드: 세로 스택
차트 카드: 전체 너비 (스크롤 가능)
인사이트 카드: 전체 너비
```

### 8.5 타이포그래피 스케일링

| 요소 | Desktop | Tablet | Mobile |
|------|---------|--------|--------|
| Display (타이머) | 60px | 54px | 48px |
| H1 | 36px | 32px | 28px |
| H2 | 30px | 26px | 24px |
| H3 | 24px | 22px | 20px |
| Body | 16px | 16px | 16px |

### 8.6 간격 (Spacing) 스케일링

| 간격 | Desktop | Tablet | Mobile |
|------|---------|--------|--------|
| Section Margin | 64px | 48px | 32px |
| Card Padding | 32px | 24px | 20px |
| Element Gap | 24px | 16px | 12px |

---

## 9. 접근성 & WCAG 2.2 체크리스트

### 9.1 색상 대비 (Color Contrast)

#### 9.1.1 텍스트 대비율

| 텍스트 크기 | 최소 대비율 | 실제 대비율 | 준수 여부 |
|------------|-----------|------------|----------|
| 본문 (16px) | 4.5:1 (AA) | 13.5:1 | ✅ AAA |
| 큰 텍스트 (24px+) | 3:1 (AA) | 13.5:1 | ✅ AAA |
| 버튼 텍스트 | 4.5:1 (AA) | 7.2:1 | ✅ AA |
| 보조 텍스트 | 4.5:1 (AA) | 4.8:1 | ✅ AA |

#### 9.1.2 UI 요소 대비

| 요소 | 배경 | 전경 | 대비율 | 준수 |
|------|------|------|--------|------|
| Primary 버튼 | `primary-600` | `white` | 7.2:1 | ✅ |
| 입력 필드 보더 | `neutral-50` | `neutral-200` | 1.5:1 | ✅ (Non-text) |
| 포커스 링 | `white` | `primary-600` | 6.5:1 | ✅ |
| 그래프 라인 | `neutral-50` | `primary-600` | 8.1:1 | ✅ |

### 9.2 키보드 네비게이션

**필수 요구사항:**
- [ ] 모든 인터랙티브 요소는 `Tab`으로 접근 가능
- [ ] 포커스 순서는 논리적 (좌→우, 상→하)
- [ ] `Enter` / `Space`로 버튼 활성화 가능
- [ ] `Esc`로 모달/드롭다운 닫기 가능
- [ ] 포커스 트랩: 모달 내부에서 순환

**단축키:**
```
Space: 타이머 시작/일시정지
Esc: 모달 닫기
/: AI 챗봇 포커스
Ctrl/Cmd + I: 인사이트 페이지
```

### 9.3 스크린 리더 지원

#### 9.3.1 Semantic HTML

```html
✅ 사용: <button>, <input>, <nav>, <main>, <section>
❌ 금지: <div> with onclick (버튼 역할)
```

#### 9.3.2 ARIA 속성

| 요소 | ARIA 속성 | 예시 |
|------|----------|------|
| 버튼 | `aria-label` | "세션 시작하기" |
| 타이머 | `aria-live="polite"` | "23분 45초 남음" |
| 집중도 점수 | `aria-label` | "현재 집중도 7점, 10점 만점" |
| 모달 | `role="dialog"`, `aria-modal="true"` | - |
| 탭 | `role="tablist"`, `aria-selected` | - |
| 프로그레스 | `role="progressbar"`, `aria-valuenow` | - |

#### 9.3.3 대체 텍스트

```html
<!-- 일러스트 -->
<img src="character.svg" alt="책을 읽고 있는 사람" />

<!-- 장식 아이콘 -->
<svg aria-hidden="true">...</svg>

<!-- 정보 아이콘 -->
<svg aria-label="집중도 상승">
  <use href="#trending-up" />
</svg>
```

### 9.4 폼 접근성

**레이블 연결:**
```html
<label for="subject-input">학습 과목</label>
<input id="subject-input" type="text" />
```

**에러 메시지:**
```html
<input 
  aria-invalid="true" 
  aria-describedby="error-subject"
/>
<p id="error-subject" role="alert">
  과목 이름을 입력해주세요
</p>
```

**필수 필드:**
```html
<label for="subject">
  학습 과목 <span aria-label="필수">*</span>
</label>
<input id="subject" required aria-required="true" />
```

### 9.5 애니메이션 & 모션

**Reduced Motion 지원:**
```css
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

**적용 대상:**
- 모달 열림/닫힘
- 페이지 전환
- 휴식 모드 배경 애니메이션
- 차트 애니메이션

### 9.6 포커스 관리

**포커스 스타일 (필수):**
```css
:focus-visible {
  outline: 2px solid var(--primary-600);
  outline-offset: 2px;
}
```

**포커스 트랩 (모달):**
```javascript
// 모달 열릴 때: 첫 포커스 가능 요소에 포커스
// Tab 키: 모달 내부 순환
// Esc 키: 모달 닫고 원래 요소로 포커스 복귀
```

### 9.7 터치 타겟 크기

| 요소 | 최소 크기 | 권장 크기 | 준수 |
|------|----------|----------|------|
| 버튼 | 44×44px | 48×48px | ✅ |
| 아이콘 버튼 | 44×44px | 48×48px | ✅ |
| 링크 (텍스트) | - | 패딩 추가 | ✅ |
| 체크박스 | 24×24px | 32×32px | ✅ |

### 9.8 언어 명시

```html
<html lang="ko">
  <head>
    <title>StudyFlow - 적응형 스터디 타이머</title>
  </head>
</html>
```

### 9.9 WCAG 2.2 체크리스트

#### Level A (필수)
- [x] 1.1.1 Non-text Content: 모든 이미지에 alt 텍스트
- [x] 1.3.1 Info and Relationships: 시맨틱 HTML 사용
- [x] 1.4.1 Use of Color: 색상만으로 정보 전달 금지
- [x] 2.1.1 Keyboard: 모든 기능 키보드 접근 가능
- [x] 2.4.1 Bypass Blocks: Skip to main content 링크
- [x] 3.2.2 On Input: 입력 시 예기치 않은 변화 없음
- [x] 4.1.2 Name, Role, Value: ARIA 속성 적절히 사용

#### Level AA (권장)
- [x] 1.4.3 Contrast (Minimum): 4.5:1 이상
- [x] 1.4.5 Images of Text: 텍스트 이미지 대신 실제 텍스트
- [x] 2.4.7 Focus Visible: 명확한 포커스 표시
- [x] 3.3.3 Error Suggestion: 에러 수정 제안 제공
- [x] 3.3.4 Error Prevention: 중요 액션 전 확인

#### Level AAA (목표)
- [x] 1.4.6 Contrast (Enhanced): 7:1 이상 (본문 텍스트)
- [ ] 2.4.8 Location: 현재 위치 표시 (Breadcrumb)
- [x] 3.2.5 Change on Request: 사용자 요청 시에만 변화

### 9.10 접근성 테스트 도구

**자동 테스트:**
- axe DevTools (Chrome Extension)
- Lighthouse Accessibility Audit
- WAVE (WebAIM)

**수동 테스트:**
- 키보드만으로 전체 네비게이션 시도
- 스크린 리더 (NVDA, JAWS, VoiceOver) 테스트
- 색각 이상 시뮬레이터 (Color Oracle)

---

## 10. 컴포넌트 명세 (Component Specifications)

### 10.1 타이머 컴포넌트 (Timer Component)

#### 10.1.1 Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `duration` | `number` | - | 초 단위 시간 |
| `isRunning` | `boolean` | `false` | 실행 상태 |
| `focusScore` | `number` | `0` | 집중도 점수 (1-10) |
| `onStart` | `function` | - | 시작 콜백 |
| `onPause` | `function` | - | 일시정지 콜백 |
| `onEnd` | `function` | - | 종료 콜백 |

#### 10.1.2 상태

| State | Value | Visual |
|-------|-------|--------|
| Idle | `isRunning: false, duration: 0` | 회색 타이머 |
| Running | `isRunning: true` | 그린 타이머, 애니메이션 |
| Paused | `isRunning: false, duration > 0` | 그린 타이머, 정지 |
| Completed | 세션 종료 | 모달 표시 |

### 10.2 프로그레스 바 (Progress Bar)

```
Props:
- value: number (0-100)
- color: 'primary' | 'accent' | 'success'
- size: 'sm' | 'md' | 'lg'
- showLabel: boolean

Styles:
- 높이: 8px (sm), 12px (md), 16px (lg)
- Border Radius: 9999px (완전 둥글게)
- 배경: neutral-200
- 채움: color에 따라
- 애니메이션: 부드러운 전환 (0.3s ease)
```

### 10.3 아바타 (Avatar)

```
Props:
- src: string (이미지 URL)
- alt: string
- size: 'sm' | 'md' | 'lg'
- fallback: string (이니셜)

Sizes:
- sm: 32×32px
- md: 48×48px
- lg: 64×64px

Styles:
- Border Radius: 50% (원형)
- Border: 2px solid neutral-200
- 폴백 배경: primary-100
- 폴백 텍스트: primary-600
```

---

## 11. 디자인 파일 관리

### 11.1 Figma 구조

```
StudyFlow Design System
├── 🎨 Color Palette
├── 📐 Typography Scale
├── 🧩 Components
│   ├── Buttons
│   ├── Cards
│   ├── Inputs
│   ├── Modals
│   └── Icons
├── 📱 Screens
│   ├── Study Mode
│   ├── Break Mode
│   ├── Insights
│   └── Settings
└── 🖼️ Illustrations
    ├── Characters
    ├── Empty States
    └── Celebrations
```

### 11.2 네이밍 컨벤션

**컴포넌트:**
```
[Category]/[Component]/[Variant]/[State]

예시:
Button/Primary/Large/Hover
Card/Stat/Default/Default
Input/Text/Error/Focused
```

**색상:**
```
[Color]/[Shade]

예시:
Primary/600
Neutral/50
Accent/500
```

### 11.3 익스포트 설정

**SVG 아이콘:**
- Format: SVG
- Include: "id"
- Outline strokes: Yes

**PNG 이미지:**
- Scale: @2x, @3x (mobile)
- Format: PNG 24

**일러스트레이션:**
- Format: SVG (preferred)
- Fallback: PNG @2x

---

## 12. 구현 가이드라인

### 12.1 CSS 변수 정의

```css
:root {
  /* Primary Colors */
  --color-primary-50: #F0F9F7;
  --color-primary-100: #D4EDE8;
  --color-primary-200: #A9DBD0;
  --color-primary-300: #7DC9B8;
  --color-primary-400: #52B7A0;
  --color-primary-500: #27A588;
  --color-primary-600: #0F5C4D;
  --color-primary-700: #0D4A3E;
  --color-primary-800: #0A382F;
  --color-primary-900: #083A2F;

  /* Accent Colors */
  --color-accent-50: #FEF2F2;
  --color-accent-100: #FADADA;
  --color-accent-200: #F5B5B5;
  --color-accent-300: #F09090;
  --color-accent-400: #EB6B6B;
  --color-accent-500: #D84A4A;
  --color-accent-600: #C43E3E;
  --color-accent-700: #A93737;
  --color-accent-800: #8E2F2F;
  --color-accent-900: #732727;

  /* Neutral Colors */
  --color-neutral-50: #F9F7F4;
  --color-neutral-100: #F2EFE9;
  --color-neutral-200: #E5E1D8;
  --color-neutral-300: #D8D3C7;
  --color-neutral-400: #CBC5B6;
  --color-neutral-500: #8B8679;
  --color-neutral-600: #6E6A5F;
  --color-neutral-700: #514E47;
  --color-neutral-800: #34322F;
  --color-neutral-900: #2A2621;

  /* Typography */
  --font-primary: 'Pretendard Variable', -apple-system, sans-serif;
  --font-mono: 'JetBrains Mono', monospace;

  /* Spacing */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  --space-10: 40px;
  --space-12: 48px;
  --space-16: 64px;

  /* Border Radius */
  --radius-sm: 4px;
  --radius-md: 8px;
  --radius-lg: 16px;
  --radius-full: 9999px;

  /* Transitions */
  --transition-fast: 0.15s ease;
  --transition-base: 0.2s ease;
  --transition-slow: 0.3s ease;
}
```

### 12.2 Tailwind Config 예시

```javascript
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#F0F9F7',
          100: '#D4EDE8',
          // ... (위 CSS 변수와 동일)
          900: '#083A2F',
        },
        accent: {
          // ...
        },
        neutral: {
          // ...
        },
      },
      fontFamily: {
        sans: ['Pretendard Variable', 'system-ui', 'sans-serif'],
        mono: ['JetBrains Mono', 'monospace'],
      },
      spacing: {
        // 4px 단위로 확장
      },
      borderRadius: {
        'sm': '4px',
        'md': '8px',
        'lg': '16px',
      },
    },
  },
};
```

### 12.3 클래스 네이밍 (BEM 스타일)

```css
.timer {
  /* 블록 */
}

.timer__display {
  /* 요소 */
}

.timer__display--paused {
  /* 수정자 */
}

.button {
  /* 블록 */
}

.button--primary {
  /* 수정자 */
}

.button--large {
  /* 수정자 */
}
```

---

## 13. 디자인 QA 체크리스트

### 13.1 시각 디자인

- [ ] 모든 텍스트는 neutral-900 사용 (순수 검정 금지)
- [ ] 배경은 neutral-50 또는 neutral-100만
- [ ] Primary 버튼은 primary-600
- [ ] 카드는 16px border-radius
- [ ] 그림자 효과 없음 (플랫 디자인 유지)
- [ ] 일러스트는 아웃라인 스타일

### 13.2 타이포그래피

- [ ] 본문은 16px Pretendard Regular
- [ ] 타이머는 60px JetBrains Mono Semibold
- [ ] 헤딩은 Pretendard Semibold (600)
- [ ] Line-height는 1.6 (본문)
- [ ] Letter-spacing: 타이머만 -0.02em

### 13.3 접근성

- [ ] 모든 대화형 요소는 포커스 스타일 있음
- [ ] 텍스트 대비율 4.5:1 이상
- [ ] 모든 이미지에 alt 텍스트
- [ ] 키보드로 전체 네비게이션 가능
- [ ] ARIA 속성 적절히 사용

### 13.4 반응형

- [ ] Desktop (1024px) 기준 디자인
- [ ] Mobile (< 768px) 최적화
- [ ] 타이머 크기: Desktop 60px, Mobile 48px
- [ ] 버튼: Desktop 가로, Mobile 세로
- [ ] 카드 패딩: Desktop 32px, Mobile 20px

### 13.5 인터랙션

- [ ] 호버 효과는 subtle (색상 변화만)
- [ ] 트랜지션 0.2s ease
- [ ] 로딩 상태는 primary-600 스피너
- [ ] 모달 애니메이션 0.2s
- [ ] 휴식 모드 배경 애니메이션 5s

---

## 14. 버전 히스토리

| 버전 | 날짜 | 변경 사항 | 작성자 |
|------|------|----------|--------|
| 1.0 | 2026-01-29 | 초기 디자인 시스템 문서 작성 | Design Team |

---

## 15. 참고 자료

### 15.1 디자인 영감
- [Calm App](https://www.calm.com) - 차분한 UI/UX
- [Forest App](https://www.forestapp.cc) - 집중 타이머
- [Notion](https://www.notion.so) - 미니멀 디자인

### 15.2 기술 문서
- [Tailwind CSS Docs](https://tailwindcss.com/docs)
- [WCAG 2.2 Guidelines](https://www.w3.org/WAI/WCAG22/quickref/)
- [Material Design Accessibility](https://m3.material.io/foundations/accessible-design)

### 15.3 폰트
- [Pretendard](https://github.com/orioncactus/pretendard)
- [JetBrains Mono](https://www.jetbrains.com/lp/mono/)

### 15.4 아이콘
- [Lucide Icons](https://lucide.dev)

---

**문서 끝**

이 디자인 가이드는 "조용한 학습 파트너"라는 제품 철학을 기반으로 작성되었습니다.  
모든 디자인 결정은 **사용자의 집중을 방해하지 않으면서도 따뜻한 지원을 제공**하는 원칙에 따릅니다.

디자인 시스템은 지속적으로 발전하며, 사용자 피드백을 반영하여 업데이트됩니다.

---

**문서 버전**: v1.0  
**최종 검토**: ✅ 완료  
**다음 리뷰**: 2026-03-01